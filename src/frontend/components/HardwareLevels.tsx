import React, { useState } from 'react';
import Box from '@mui/material/Box';
import { useTheme } from '@mui/material/styles';

const HardwareLevels = (props: any) => {
    const theme = useTheme();

    return (
        <Box></Box>
    );
};

export default HardwareLevels;